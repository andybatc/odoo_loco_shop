apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: odoo-shop
spec:
  selector:
    app: odoo-shop
    tier: db
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
