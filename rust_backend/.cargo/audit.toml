[advisories]
ignore = [
    # rsa 0.9.10: Marvin Attack timing sidechannel (RUSTSEC-2023-0071).
    # No patched version available. rsa is an indirect dependency via
    # sea-orm -> sqlx -> sqlx-mysql -> rsa. We only use SQLite and
    # PostgreSQL, never MySQL, but sqlx-mysql is a hard dependency of
    # sqlx v0.8 and cannot be disabled.
    "RUSTSEC-2023-0071",
]
